{
  "name": "ar-world-map",
  "private": true,
  "version": "1.0.0",
  "description": "WebAR world-map experience (MindAR + Three.js) with an admin dashboard, hosted on Netlify.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:full": "npm run compile-target && npm run build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.node.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "compile-target": "node scripts/compile-target.mjs",
    "netlify:dev": "node scripts/netlify-dev.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9",
    "mind-ar": "^1.2.5",
    "netlify-cli": "^26.2.0",
    "three": "0.160.0"
  },
  "devDependencies": {
    "@netlify/functions": "^5.3.0",
    "@types/node": "^26.1.1",
    "@types/three": "0.185.1",
    "puppeteer": "^25.3.0",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "canvas": "npm:@napi-rs/canvas@^0.1.0"
  },
  "allowScripts": {
    "puppeteer@25.1.0": true,
    "core-js@3.29.1": true,
    "esbuild@0.25.12": true
  }
}
